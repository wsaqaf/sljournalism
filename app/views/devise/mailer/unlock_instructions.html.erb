<p><%= t('hello') %> <%= @resource.email %>!</p>

<p><%= t('devise.account.acc_locked') %></p>

<p><%= t('devise.account.unlock_acc_link') %>:</p>

<p><%= link_to t('devise.account.unlock_acc'), unlock_url(@resource, unlock_token: @token) %></p>
